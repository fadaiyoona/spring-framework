自定义一个<msb:user   username email age>,完成整个标签的处理工作

spring的处理流程
1、创建一个PluggableSchemaResolver，它是sax规范，EntityResolver接口，用于配合DocumentLoader解析配置文件成类对象，如Document对象
@see org.springframework.beans.factory.xml.PluggableSchemaResolver.getSchemaMappings，
会将所有的META-INF/spring.schemas文件，解析成map，schema URL -> local schema path。避免从网络上下载这个xsd文件
2、初始化一个DefaultNamespaceHandlerResolver，
@see org.springframework.beans.factory.xml.DefaultNamespaceHandlerResolver.getHandlerMappings
将所有的META-INF/spring.handlers文件，解析成map，Stores the mappings from namespace URI to NamespaceHandler class name / instance.
3、调用NamespaceHandler的init方法，将自定义命名空间的处理类进行初始化，将自定义标签，对应的parser处理类注册好，
@see org.springframework.context.config.ContextNamespaceHandler
4、调用
@see org.springframework.beans.factory.xml.NamespaceHandlerSupport.parse
进行解析Element对象成BeanDefinition，子类实现doParse来做自定义标签的处理

自定义标签的流程
1、创建META-INF/Spring.schemas文件
@see META-INF/Spring.schemas
2、创建META-INF/Spring.handlers文件
@see META-INF/Spring.handlers
3、创建一个NamespaceHandler对象，
@see com.selftag.UserNamespaceHandler
4、创建一个解析处理类BeanDefinitionParser
@see com.selftag.UserBeanDefinitionParser
